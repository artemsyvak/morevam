export namespace SpotifyApi {
    export interface ListOfUsersPlaylistsResponse {

        // Define the structure of ListOfUsersPlaylistsResponse
    }

    export interface CreatePlaylistResponse {
        // Define the structure of CreatePlaylistResponse
    }

    export interface SnapshotResponse {
        // Define the structure of SnapshotResponse
    }

    export interface TrackSearchResponse {
        // Define the structure of TrackSearchResponse
    }
}